import maxime.Map
import maxime.Option
import maxime.String

class Node
  Node(parent: Option[Node], localName: String, attrs: Map[String, String])
  attr(name: String) -> Option[String] = attrs.get(name)
