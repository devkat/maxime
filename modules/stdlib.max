/*
type JSObj {
  def attr (o: JSObj, name: String) -> Option[String] =
    `o.getAttribute(name)`
}
*/

feature Functor[f]
  map(func: a -> b) -> f[b]


class Pair[a, b]
  Pair(fst: a, snd: b)
  fst(p: Pair[a, b]) -> a = p.fst
  snd(p: Pair[a, b]) -> b = p.snd
