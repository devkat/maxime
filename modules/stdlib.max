/*
type JSObj {
  def attr (o: JSObj, name: String) -> Option[String] =
    `o.getAttribute(name)`
}
*/

class Functor[f]
  def map (functor: f[a], func: a -> b) -> f[b]


class Pair[a, b]
  Pair(fst: a, snd: b)
  def fst (p: Pair[a, b]) -> a = p.fst
  def snd (p: Pair[a, b]) -> b = p.snd
