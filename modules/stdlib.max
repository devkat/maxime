/*
type JSObj {
  def attr (o: JSObj, name: String) -> Option[String] =
    `o.getAttribute(name)`
}
*/

feature Functor[f]
  map(func: a -> b) -> f[b]


class Pair[a, b]
  Pair(first: a, second: b)
  fst() -> a = first
  snd() -> b = second
