class Compiler
  Compiler()
  def compile (sourceArray: List[File], target: File, options: Map[String, Any]) → Unit =
    val sources = array2list(sourceArray)
    println("sources: " + toJson(sources) + ", " + sourceArray)
    println("Compiling " + join(list_map(sources, toString), ", ") + " to " + target)